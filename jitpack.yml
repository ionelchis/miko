jdk:
  - openjdk21

before_install:
  - chmod +x gradlew
  - ./gradlew clean

build:
  - ./gradlew clean build publishToMavenLocal -x test

cache:
  paths:
    - ~/.gradle/caches
    - ~/.gradle/wrapper
